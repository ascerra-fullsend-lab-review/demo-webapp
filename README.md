# Task Tracker

A minimal task tracker web app for testing the fullsend outcome review agent.

## Usage

Open `index.html` in a browser or serve with:

```bash
python3 -m http.server 8080
```

Then visit http://localhost:8080.
